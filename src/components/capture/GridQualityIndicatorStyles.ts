import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  qualityContainer: {
    width: '100%',
    marginTop: 8,
  },
  qualityBar: {
    height: 8,
    borderRadius: 4,
    marginBottom: 4,
  },
  qualityText: {
    color: 'white',
    fontSize: 12,
    textAlign: 'center'
  }
});

export default styles;